services:
  agent:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/seen_articles.db
      BOOTSTRAP_ON_START: "true"
    volumes:
      - agent-data:/app/data
    command: ["run"]

volumes:
  agent-data:
